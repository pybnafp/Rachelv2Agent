# Rachel-v2 Web 平台 M2（前端 + 结果可视化）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** React 前端（登录/提交/任务列表/任务详情）+ 路线树画布（React Flow 渲染 visualization.json 二部图）+ 报告嵌入，外加支撑它的两个后端端点（/result、/files）与 export 落位修正。

**Architecture:** Vite + React 18 + TS 单页应用，TanStack Query 管服务端状态（running 态 3s 轮询），Zustand 管 JWT，RDKit-JS WASM 本地渲染 SMILES；后端仅增两个只读端点。开发期 Vite proxy `/api`→uvicorn:8000，无 CORS。

**Tech Stack:** Node v24.14.0 / npm 11.17.0（已验证）、Vite 5、React 18、TypeScript 5、TailwindCSS 3.4、@xyflow/react 12、@tanstack/react-query 5、zustand 4、@rdkit/rdkit（npm）、Vitest + @testing-library/react。

**Spec:** `docs/superpowers/specs/2026-08-17-rachel-web-platform-design.md` §5 前端设计 + §10 M2 行。M1 已交付（main 分支，51 测试绿）。

## Global Constraints

- 后端命令在 rachel-v2 env：`D:/Anaconda/envs/rachel-v2/python.exe -m pytest backend/tests -v`（conda 不在 PATH）。
- 前端命令：`npm --prefix frontend run <script>`（或 cd frontend && npm run）；Node 已装。
- **`Rachel-v2/` 目录只读**；本计划唯一例外：M2-T1 修改 `backend/app/agent/driver.py`（后端自己的文件，非算法端）。
- 后端测试不依赖外部服务（SQLite + eager + mock）；前端测试 Vitest+jsdom，mock fetch 与 RDKit 模块，不起浏览器。
- UI 组件不用 shadcn CLI（交互式 init 有网络风险）——按 shadcn 模式**手写** `src/components/ui/*`（Button/Badge/Card/Input/Tabs，Tailwind 实现）。此为对 spec "shadcn/ui" 的落地方式裁定。
- 错误响应契约 `{"error": ...}`；任务状态枚举 `queued/running/succeeded/partial/failed/cancelled`。
- 每任务一 commit（conventional）；TDD：先失败测试再实现（前端组件测试同样要求）。
- npm 若因网络装包失败：优先重试/换镜像 `npm config get registry`（用户可能已配国内镜像）；@rdkit/rdkit 装不动的兜底 = 从 unpkg 手动下载 `RDKit_minimal.js`/`RDKit_minimal.wasm` 放 `frontend/public/rdkit/`（plan 内 postinstall 本来就会拷到这）。

## M1 实测接口契约（前端消费的事实基准，已对照代码核实）

```
POST /api/auth/register {username, password} → 201 {access_token, role}   # 首用户 admin
POST /api/auth/login    {username, password} → 200 {access_token, role}
GET  /api/auth/me                    → {id, username, role}
POST /api/jobs {smiles, name?}       → 201 JobOut（smiles 已规范化）
GET  /api/jobs?mine=1&page=1         → JobOut[]（按 created_at 倒序, 20/页）
GET  /api/jobs/{id}                  → JobOut
GET  /api/jobs/{id}/trace?after=n    → {steps: JobStepOut[]}   # M3 消费
POST /api/jobs/{id}/cancel           → {ok, status}（非 queued/running → 409）
DELETE /api/jobs/{id}                → {ok}
GET  /api/admin/llm-providers        → ProviderOut[]（无 api_key 字段；admin only）

JobOut = {id, smiles, name, status, error, stats: dict, created_at, started_at|null, finished_at|null}
stats 实测键：{steps, tokens_in, tokens_out, reason?, export_dir?(成功时), artifacts?: {n_nodes, n_edges, n_terminals, incomplete}?}
401 响应体：{"error": "missing token" | "invalid token" | ...}
```

**本计划新增端点（M2-T1/T2 交付）**：

```
GET /api/jobs/{id}/result → 200 {
  job: JobOut,
  visualization?: {nodes: VisNode[], edges: VisEdge[], meta: {...}},   # 文件缺失时缺省
  terminals?: [...],
  metrics?: {n_nodes, n_edges, n_terminals, incomplete}
}   # 产物未就绪（running/failed/无 export_dir）→ 200 仅含 job（前端按缺省键降级），越权 404

GET /api/jobs/{id}/files/{path} → 文件流（root = data_dir/{job_id}）
  # path 相对工作区根；.. 穿越 → 404；Content-Type 按后缀
  # export 产物位于工作区下 export/ 子目录（M2-T1 把 driver export 改到 workspace/export）

VisNode（实测自 examples/result_demo/export/visualization.json）:
  molecule: {id: "mol_0", type: "molecule", smiles, role: "target|intermediate|terminal", depth: 0, cs_score: 4.4, classification: "moderate"}
  reaction: {id: "rxn_1", type: "reaction", label: "Reductive amination", depth: 0, reaction_smices: "A.B>>C"}
VisEdge: {source: "mol_0", target: "rxn_1", type: "retro_product"}
meta: {target, target_name, status, total_steps, total_molecules}
```

## File Structure（M2 全量文件图）

```
backend/
├── app/agent/driver.py                  # M2-T1 改: export output_dir → workspace/export
├── app/api/jobs.py                      # M2-T1/T2 增: /result /files
├── app/schemas/jobs.py                  # M2-T1 增: ResultOut
├── tests/test_m2_result_files.py        # 两任务的测试
frontend/
├── package.json / vite.config.ts / tsconfig.json / tailwind.config.js / postcss.config.js
├── index.html                           # 含 /rdkit/RDKit_minimal.js script 标签
├── public/rdkit/                        # postinstall 从 node_modules/@rdkit/rdkit 拷入 WASM
├── src/
│   ├── main.tsx / App.tsx               # 路由 + QueryClientProvider + AuthGuard
│   ├── index.css                        # Tailwind + 设计 token（蓝青浅色主题）
│   ├── api/client.ts                    # fetch 封装: Bearer 注入、401→清 token 跳登录、{"error"} 抛错
│   ├── api/hooks.ts                     # useMe/useJobs/useJob/useResult/useSubmit/useCancel/useDelete
│   ├── stores/auth.ts                   # zustand persist: token/role/user
│   ├── components/ui/{Button,Badge,Card,Input,Tabs}.tsx   # 手写 shadcn 风格
│   ├── components/MoleculeView.tsx      # RDKit-JS 渲染 SMILES→SVG，失败回退文本
│   ├── components/StatusBadge.tsx       # 六状态配色
│   ├── components/RouteTreeCanvas.tsx   # ★ React Flow 画布（布局/配色/点击）
│   ├── components/NodeDrawer.tsx        # 节点/边详情抽屉
│   ├── pages/LoginPage.tsx / RegisterPage.tsx
│   ├── pages/SubmitPage.tsx             # 双栏：SMILES+预览 / 高级折叠
│   ├── pages/JobsPage.tsx               # 表格+轮询+操作
│   ├── pages/JobDetailPage.tsx          # 头部+Tab（路线树/报告/文件）+ running/failed 形态
│   └── tests/                           # *.test.tsx 与 fixtures/visualization.json
└── backend/README.md                    # M2-T11 追加前端段落
```

**跨任务接口契约**（后续任务引用）：

```ts
// api/client.ts
export async function api<T>(path: string, init?: RequestInit & {json?: unknown}): Promise<T>
// GET 便捷: apiGet<T>(path); 挂 Bearer；401 → authStore.logout() + 跳 /login；非 2xx → throw Error(body.error)
// stores/auth.ts
useAuthStore: {token: string|null, role: string|null, login(t, r): void, logout(): void}  // persist key "rachel-auth"
// components/MoleculeView.tsx
export function MoleculeView({smiles, width?, height?}: {smiles: string; width?: number; height?: number})
// components/RouteTreeCanvas.tsx
export function RouteTreeCanvas({vis}: {vis: {nodes: VisNode[]; edges: VisEdge[]}})
// types.ts（src/types.ts 集中放 VisNode/VisEdge/Job/JobOut/ResultOut 前端类型）
```

---

### Task M2-T1: 后端 /result 端点 + driver export 落位修正

**Files:**
- Modify: `backend/app/agent/driver.py`（_finalize_export 传 output_dir）
- Modify: `backend/app/api/jobs.py`、`backend/app/schemas/jobs.py`
- Test: `backend/tests/test_m2_result_files.py`（本任务先写 /result 部分）

**Interfaces:**
- Consumes: `parse_export(Path)`（T10，返回 visualization/terminals/metrics/incomplete）、`_get_own_job`、Job.stats
- Produces: `GET /api/jobs/{id}/result`（契约见上）；driver 行为变更：export 调用改为 `{"name": self.name, "output_dir": str(self.workspace / "export")}`（workspace 为 None 时保持只传 name——FakeRetroCmd 单测路径）

- [ ] **Step 1: 失败测试（driver export 落位）**

```python
def test_driver_exports_into_workspace(tmp_path):
    from tests.fakes import FakeRetroCmd, ListTrace
    from app.agent.llm_client import ToolCall, MockLLMClient
    from app.agent.driver import AgentDriver
    retro = FakeRetroCmd()
    d = AgentDriver(retro=retro, llm=MockLLMClient(script=[[ToolCall("c1", "finish", {"summary": "x"})]]),
                    trace=ListTrace(), task_prompt="t", name="n", workspace=tmp_path)
    d.run()
    export_args = [a for c, a in retro.calls if c == "export"][0]
    assert export_args["output_dir"] == str(tmp_path / "export")
```

- [ ] **Step 2: 失败测试（/result）**

```python
PARA = "CC(=O)Nc1ccc(O)cc1"

def test_result_aggregates_artifacts(client, db, auth_headers_user, tmp_path, monkeypatch):
    import json as _json
    from pathlib import Path
    import app.core.config as cfg
    monkeypatch.setattr(cfg.Settings, "data_dir", tmp_path, raising=False)
    # 提交（eager + mock LLM provider → 立即 finish → driver export 到 workspace/export）
    from app.db.models import LlmProvider
    from sqlalchemy import select
    p = db.scalar(select(LlmProvider)); p.model = "mock"; db.commit()
    jid = client.post("/api/jobs", headers=auth_headers_user,
                      json={"smiles": PARA, "name": "para"}).json()["id"]
    export = tmp_path / jid / "export"; export.mkdir(parents=True, exist_ok=True)
    src = Path("examples/result_demo/export")
    for f in ("visualization.json", "terminals.json"):
        (export / f).write_text((src / f).read_text(encoding="utf-8"), encoding="utf-8")
    r = client.get(f"/api/jobs/{jid}/result", headers=auth_headers_user)
    assert r.status_code == 200
    body = r.json()
    assert body["job"]["id"] == jid
    assert len(body["visualization"]["nodes"]) == 17          # demo fixture 真实规模
    assert body["metrics"]["n_nodes"] == 17
    assert "terminals" in body

def test_result_degrades_without_artifacts(client, db, auth_headers_user):
    from app.db.models import Job, User
    u = db.query(User).first()
    db.add(Job(id="jx", user_id=u.id, smiles="CCO", status="running")); db.commit()
    r = client.get("/api/jobs/jx/result", headers=auth_headers_user)
    assert r.status_code == 200 and r.json()["job"]["id"] == "jx"
    assert "visualization" not in r.json()

def test_result_other_user_404(client, db, auth_headers_admin, auth_headers_user):
    jid = client.post("/api/jobs", headers=auth_headers_user, json={"smiles": "CCO"}).json()["id"]
    client.post("/api/auth/register", json={"username": "m2eve", "password": "pw9"})
    tok = client.post("/api/auth/login", json={"username": "m2eve", "password": "pw9"}).json()["access_token"]
    assert client.get(f"/api/jobs/{jid}/result",
                      headers={"Authorization": f"Bearer {tok}"}).status_code == 404
```

- [ ] **Step 3: driver.py 最小改**

```python
def _finalize_export(self) -> dict:
    if not self._finalized:
        try:
            self.retro.execute("finalize", {"summary": f"auto-finalize ({self.name})"})
        except Exception:
            pass
        self._finalized = True
    args = {"name": self.name}
    if self.workspace is not None:
        args["output_dir"] = str(self.workspace / "export")
    try:
        r = self.retro.execute("export", args)
        return r if isinstance(r, dict) else {}
    except Exception:
        return {}
```

- [ ] **Step 4: /result 实现**

`schemas/jobs.py` 追加：

```python
class ResultOut(BaseModel):
    job: JobOut
    visualization: dict | None = None
    terminals: list | None = None
    metrics: dict | None = None
```

`api/jobs.py` 追加：

```python
from pathlib import Path
from app.schemas.jobs import ResultOut
from app.services.artifacts import parse_export

@router.get("/{job_id}/result", response_model=ResultOut)
def result(job_id: str, user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    job = _get_own_job(db, job_id, user)
    out = ResultOut(job=JobOut.model_validate(job))
    export_dir = (job.stats or {}).get("export_dir")
    if export_dir and Path(export_dir).exists():
        parsed = parse_export(Path(export_dir))
        out.visualization = parsed.get("visualization")
        out.terminals = parsed.get("terminals")
        out.metrics = parsed.get("metrics")
    return out
```

注意：worker 侧 stats["export_dir"] 已取自 `result.export_result["output_dir"]`（T6/T11 行为），driver 改造后该值即 `data_dir/{id}/export`，/result 与 /files（T2）同根。

- [ ] **Step 5: 跑测试**

Run: `D:/Anaconda/envs/rachel-v2/python.exe -m pytest backend/tests/test_m2_result_files.py -v` 然后 `... -m pytest backend/tests -v`
Expected: 新测试 PASS + 全套（51+新增）PASS（含既有 T9/T11 测试不回归）

- [ ] **Step 6: Commit**

```bash
git add backend/app backend/tests/test_m2_result_files.py
git commit -m "feat: job result aggregation endpoint and workspace-scoped export"
```

---

### Task M2-T2: 后端 /files 文件服务端点

**Files:**
- Modify: `backend/app/api/jobs.py`
- Test: `backend/tests/test_m2_result_files.py`（追加）

**Interfaces:**
- Consumes: `_get_own_job`、settings.data_dir
- Produces: `GET /api/jobs/{id}/files/{path}`（workspace 根相对路径；FileResponse；穿越 404；Content-Type 映射 .html/.png/.json/.txt/.md/其他 octet-stream）

- [ ] **Step 1: 失败测试**

```python
def test_files_serves_and_blocks_traversal(client, db, auth_headers_user, tmp_path, monkeypatch):
    import app.core.config as cfg
    monkeypatch.setattr(cfg.Settings, "data_dir", tmp_path, raising=False)
    jid = client.post("/api/jobs", headers=auth_headers_user, json={"smiles": "CCO"}).json()["id"]
    ws = tmp_path / jid; ws.mkdir(parents=True, exist_ok=True)
    (ws / "messages.jsonl").write_text('{"a":1}\n', encoding="utf-8")
    exp = ws / "export"; exp.mkdir(); (exp / "SYNTHESIS_REPORT.html").write_text("<html>r</html>", encoding="utf-8")
    ok1 = client.get(f"/api/jobs/{jid}/files/messages.jsonl", headers=auth_headers_user)
    assert ok1.status_code == 200 and ok1.headers["content-type"].startswith("text/plain")
    ok2 = client.get(f"/api/jobs/{jid}/files/export/SYNTHESIS_REPORT.html", headers=auth_headers_user)
    assert ok2.status_code == 200 and "text/html" in ok2.headers["content-type"]
    bad = client.get(f"/api/jobs/{jid}/files/../../etc/passwd", headers=auth_headers_user)
    assert bad.status_code in (404, 400)
    other = client.get(f"/api/jobs/{jid}/files/nope.bin", headers=auth_headers_user)
    assert other.status_code == 404
```

- [ ] **Step 2: 实现（api/jobs.py）**

```python
from fastapi.responses import FileResponse

_CONTENT_TYPES = {".html": "text/html", ".png": "image/png", ".json": "application/json",
                  ".txt": "text/plain", ".md": "text/markdown", ".jsonl": "application/json"}

@router.get("/{job_id}/files/{file_path:path}")
def files(job_id: str, file_path: str, user: User = Depends(get_current_user), db: Session = Depends(get_db)):
    job = _get_own_job(db, job_id, user)
    root = (get_settings().data_dir / job.id).resolve()
    target = (root / file_path).resolve()
    if target != root and root not in target.parents:
        raise HTTPException(404, "file not found")
    if not target.is_file():
        raise HTTPException(404, "file not found")
    suffix = target.suffix.lower()
    return FileResponse(target, media_type=_CONTENT_TYPES.get(suffix, "application/octet-stream"))
```

- [ ] **Step 3: 跑测试**（同 T1 命令；全套不回归）
- [ ] **Step 4: Commit** `feat: job artifact file serving with traversal guard`

---

### Task M2-T3: 前端脚手架（Vite+React+TS+Tailwind+路由+API client+Auth store）

**Files:**
- Create: `frontend/` 全套配置 + `src/{main,App,index.css,types.ts}` + `src/api/{client,hooks}.ts` + `src/stores/auth.ts` + `src/tests/setup.ts` + `vitest.config`（并入 vite.config）
- Test: `frontend/src/tests/client.test.ts`、`frontend/src/tests/router.test.tsx`

**Interfaces:**
- Produces: 契约区 `api()/apiGet()`、`useAuthStore`、`App` 路由骨架（/login /register / /jobs /jobs/:id 占位页）、`frontend/src/types.ts` 全部前端类型

- [ ] **Step 1: 脚手架命令**

```bash
cd D:/DevelopAndGrowth/Agents/Rachelv2Agent
npm create vite@latest frontend -- --template react-ts
cd frontend && npm install
npm install @tanstack/react-query zustand react-router-dom @xyflow/react
npm install -D tailwindcss@3.4 postcss autoprefixer vitest jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event
```

`tailwind.config.js` + `postcss.config.js` 标准 Tailwind3 配置；`src/index.css` 为 `@tailwind base; @tailwind components; @tailwind utilities;` + 设计 token（CSS 变量：primary #0ea5e9 sky-500 / 深蓝 #1e40af / 背景 slate-50）。`vite.config.ts`：

```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
export default defineConfig({
  plugins: [react()],
  server: { proxy: { "/api": "http://localhost:8000" } },
  test: { environment: "jsdom", setupFiles: "./src/tests/setup.ts", globals: true },
} satisfies any);
```

（vitest 类型：`/// <reference types="vitest" />` 置顶。）

- [ ] **Step 2: types.ts（对齐后端契约，逐字段）**

```ts
export type JobStatus = "queued" | "running" | "succeeded" | "partial" | "failed" | "cancelled";
export interface JobOut {
  id: string; smiles: string; name: string; status: JobStatus; error: string;
  stats: Record<string, any>; created_at: string; started_at: string | null; finished_at: string | null;
}
export interface VisMolNode { id: string; type: "molecule"; smiles: string; role: "target" | "intermediate" | "terminal"; depth: number; cs_score?: number; classification?: string; }
export interface VisRxnNode { id: string; type: "reaction"; label: string; depth: number; reaction_smiles?: string; }
export type VisNode = VisMolNode | VisRxnNode;
export interface VisEdge { source: string; target: string; type?: string; }
export interface Visualization { nodes: VisNode[]; edges: VisEdge[]; meta?: Record<string, any>; }
export interface ResultOut { job: JobOut; visualization?: Visualization; terminals?: any[]; metrics?: Record<string, number | boolean>; }
```

- [ ] **Step 3: client.ts + auth store（完整实现）**

```ts
// stores/auth.ts
import { create } from "zustand";
import { persist } from "zustand/middleware";
interface AuthState { token: string | null; role: string | null;
  login: (token: string, role: string) => void; logout: () => void; }
export const useAuthStore = create<AuthState>()(persist((set) => ({
  token: null, role: null,
  login: (token, role) => set({ token, role }),
  logout: () => set({ token: null, role: null }),
}), { name: "rachel-auth" }));

// api/client.ts
import { useAuthStore } from "../stores/auth";
export class ApiError extends Error { constructor(public status: number, message: string) { super(message); } }
export async function api<T>(path: string, init: RequestInit & { json?: unknown } = {}): Promise<T> {
  const { json, ...rest } = init;
  const headers: Record<string, string> = { ...(rest.headers as any) };
  const token = useAuthStore.getState().token;
  if (token) headers["Authorization"] = `Bearer ${token}`;
  if (json !== undefined) headers["Content-Type"] = "application/json";
  const resp = await fetch(path, { ...rest, headers, body: json !== undefined ? JSON.stringify(json) : rest.body });
  if (resp.status === 401 && !path.startsWith("/api/auth/")) {
    useAuthStore.getState().logout();
    window.location.href = "/login";
    throw new ApiError(401, "unauthorized");
  }
  const body = await resp.json().catch(() => ({}));
  if (!resp.ok) throw new ApiError(resp.status, (body as any).error ?? `HTTP ${resp.status}`);
  return body as T;
}
export const apiGet = <T>(path: string) => api<T>(path);
```

- [ ] **Step 4: App.tsx 路由 + AuthGuard + 占位页**

```tsx
// App.tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { BrowserRouter, Routes, Route, Navigate } from "react-router-dom";
import { useAuthStore } from "./stores/auth";
const qc = new QueryClient({ defaultOptions: { queries: { retry: 1, refetchOnWindowFocus: false } } });
function Guard({ children }: { children: React.ReactNode }) {
  const token = useAuthStore((s) => s.token);
  return token ? <>{children}</> : <Navigate to="/login" replace />;
}
export default function App() {
  return (
    <QueryClientProvider client={qc}>
      <BrowserRouter>
        <Routes>
          <Route path="/login" element={<div data-testid="page-login">login</div>} />
          <Route path="/register" element={<div data-testid="page-register">register</div>} />
          <Route path="/" element={<Guard><div data-testid="page-submit">submit</div></Guard>} />
          <Route path="/jobs" element={<Guard><div data-testid="page-jobs">jobs</div></Guard>} />
          <Route path="/jobs/:id" element={<Guard><div data-testid="page-job-detail">detail</div></Guard>} />
        </Routes>
      </BrowserRouter>
    </QueryClientProvider>
  );
}
```

- [ ] **Step 5: 失败测试 → 实现 → 通过**

`client.test.ts`：无 token 不带 Authorization 头；有 token 带头；非 2xx 且 body={"error":"x"} → 抛 ApiError("x")（`vi.stubGlobal("fetch", ...)`）。
`router.test.tsx`：token=null 时渲染 `<App/>`（MemoryRouter 已由 BrowserRouter 包裹，直接 render App）访问 "/" 出现跳转（无 page-submit）；设 token 后出现 page-submit。（测试里 mock window.location 或用 jsdom 直接允许赋值 href。）

Run: `npm --prefix frontend test -- --run`
Expected: 先 FAIL（文件缺失/占位），实现后 PASS

- [ ] **Step 6: 构建 + Commit**

Run: `npm --prefix frontend run build`（tsc + vite build 通过）
```bash
git add frontend
git commit -m "feat: frontend scaffold with router, api client and auth store"
```

---

### Task M2-T4: UI 基件 + MoleculeView（RDKit-JS 集成）

**Files:**
- Create: `frontend/src/components/ui/{Button,Badge,Card,Input,Tabs}.tsx`、`frontend/src/components/{MoleculeView,StatusBadge}.tsx`、`frontend/scripts/copy-rdkit.mjs`、`frontend/index.html` 修改
- Test: `frontend/src/tests/molecule-view.test.tsx`、`status-badge.test.tsx`

**Interfaces:**
- Produces: 契约区 `MoleculeView({smiles,width?,height?})`；`StatusBadge({status})`；RDKit 资源在 `public/rdkit/`，`index.html` 挂 `<script src="/rdkit/RDKit_minimal.js"></script>`，`src/rdkit.ts` 导出 `getRDKit(): Promise<any>`（window.initRDKitModule + locateFile 指向 /rdkit/RDKit_minimal.wasm，模块级单例 promise）

- [ ] **Step 1: RDKit 资源落地**

`npm --prefix frontend install @rdkit/rdkit`；`frontend/scripts/copy-rdkit.mjs`：

```js
import { cpSync, mkdirSync } from "node:fs";
import { dirname, join } from "node:path";
const src = join(dirname(process.cwd()), "node_modules", "@rdkit", "rdkit", "Code", "MinimalLib", "dist");
```

（脚本在 frontend 目录下运行时 cwd 即 frontend：`const src = join(process.cwd(), "node_modules", "@rdkit", "rdkit", "Code", "MinimalLib", "dist"); mkdirSync("public/rdkit", {recursive: true}); cpSync(src, "public/rdkit", {recursive: true});`）
package.json scripts：`"postinstall": "node scripts/copy-rdkit.mjs"`、test/build 不变。运行 `npm --prefix frontend run postinstall` 验证 `public/rdkit/RDKit_minimal.wasm` 存在。`public/rdkit/` 提交进 git（~10MB，换取离线确定性；.gitignore 不忽略它）。

`index.html` `<head>` 内加 `<script src="/rdkit/RDKit_minimal.js"></script>`。

- [ ] **Step 2: rdkit.ts + MoleculeView（完整实现）**

```ts
// src/rdkit.ts
let ready: Promise<any> | null = null;
export function getRDKit(): Promise<any> {
  if (!ready) {
    ready = (window as any).initRDKitModule({ locateFile: (f: string) => `/rdkit/${f}` });
  }
  return ready;
}

// components/MoleculeView.tsx
import { useEffect, useState } from "react";
import { getRDKit } from "../rdkit";
export function MoleculeView({ smiles, width = 200, height = 140 }: { smiles: string; width?: number; height?: number }) {
  const [svg, setSvg] = useState<string | null>(null);
  const [failed, setFailed] = useState(false);
  useEffect(() => {
    let cancelled = false;
    setSvg(null); setFailed(false);
    getRDKit().then((RDKit) => {
      if (cancelled) return;
      const mol = RDKit.get_mol(smiles);
      if (!mol || !mol.is_valid()) { setFailed(true); mol?.delete(); return; }
      const s = mol.get_svg(width, height);
      mol.delete();
      setSvg(s);
    }).catch(() => !cancelled && setFailed(true));
    return () => { cancelled = true; };
  }, [smiles, width, height]);
  if (failed) return <div className="text-xs text-red-500 break-all" data-testid="mol-invalid">{smiles}</div>;
  if (!svg) return <div className="animate-pulse bg-slate-100 rounded" style={{ width, height }} data-testid="mol-loading" />;
  return <div data-testid="mol-svg" dangerouslySetInnerHTML={{ __html: svg }} />;
}
```

- [ ] **Step 3: ui 基件（shadcn 风格，Tailwind 类即可，每个 30 行内）**

Button（variant: primary/outline/ghost/danger，size sm/md）、Badge（颜色 prop）、Card（div 包装 + 标题）、Input（label+input+错误行）、Tabs（受控 tabs：props tabs:[{key,label}], active, onChange；无 Radix 依赖）。

- [ ] **Step 4: StatusBadge（六状态→色）**

queued=slate、running=sky（含 pulse 动画）、succeeded=emerald、partial=amber、failed=red、cancelled=zinc，图标用内联 SVG 圆点。

- [ ] **Step 5: 测试（RDKit mock）**

`status-badge.test.tsx`：六状态渲染对应 data-testid=`status-<s>`。
`molecule-view.test.tsx`：`vi.mock("../rdkit", ...)` 返回 fake getRDKit（get_mol→{is_valid:()=>true,get_svg:()=>
"<svg/>",delete(){}}）→ 断言 mol-svg 出现；is_valid false → mol-invalid；getRDKit reject → mol-invalid。

Run: `npm --prefix frontend test -- --run` → PASS；`npm run build` 通过
- [ ] **Step 6: Commit** `feat: ui primitives, rdkit molecule view and status badge`

---

### Task M2-T5: 登录/注册页

**Files:**
- Create: `frontend/src/pages/{LoginPage,RegisterPage}.tsx`（+ 共用 AuthForm 组件）
- Test: `frontend/src/tests/auth-pages.test.tsx`

**Interfaces:**
- Consumes: api client、useAuthStore、ui 基件
- Produces: 两个页面：表单校验（用户名≥2、密码≥6）→ POST /api/auth/{register|login} → login(token,role) → navigate("/")；错误显示 body.error；页间切换链接

- [ ] **Step 1: 失败测试**

stub fetch：成功 `{access_token:"t",role:"user"}` → 断言 store.token==="t" 且跳转（用 react-router 的 useNavigate 后 LocationDisplay 或 mock）；401 body {"error":"invalid credentials"} → 页面出现该文案；密码过短 → 提交按钮禁用/错误提示且 fetch 未被调用。

- [ ] **Step 2: 实现 → 通过 → build**
- [ ] **Step 3: Commit** `feat: login and register pages`

---

### Task M2-T6: 提交页（SMILES 输入 + 实时预览 + 示例）

**Files:**
- Create: `frontend/src/pages/SubmitPage.tsx`
- Test: `frontend/src/tests/submit-page.test.tsx`

**Interfaces:**
- Consumes: MoleculeView、api（POST /api/jobs）、useMe（角色显示）
- Produces: 布局=左栏主表单（任务名 Input + SMILES Textarea + MoleculeView 预览 + 有效性提示）+ 右栏折叠高级区（LLM 供应商只读列表 GET /api/admin/llm-providers，admin 可见；普通用户隐藏）+ 底部 Submit；示例分子按钮组：阿司匹林 CC(=O)OC1=CC=CC=C1C(=O)O、扑热息痛 CC(=O)Nc1ccc(O)cc1、苯 c1ccccc1；提交成功 navigate(`/jobs/${id}`)；400 {"error":...} 显示错误横幅

**有效性判定**：用 getRDKit().get_mol(smiles).is_valid()（预览组件已做渲染，此处独立调用判定，输入 300ms 防抖）；空串=中性提示。**注意 jsdom 测试 mock rdkit**。

- [ ] **Step 1: 失败测试**（mock fetch + mock MoleculeView：填 SMILES 点示例 → 输入框值变化；submit → POST 体 {smiles, name}；错误横幅）
- [ ] **Step 2: 实现 → 通过 → build**
- [ ] **Step 3: Commit** `feat: submit page with live smiles preview`

---

### Task M2-T7: 任务列表页

**Files:**
- Create: `frontend/src/pages/JobsPage.tsx`、`frontend/src/api/hooks.ts`（补齐 useJobs/useJob/useResult/useSubmit/useCancel/useDelete）
- Test: `frontend/src/tests/jobs-page.test.tsx`

**Interfaces:**
- Consumes: api client、StatusBadge、MoleculeView（小尺寸 120x80）、hooks
- Produces: `useJobs()`（GET /api/jobs?mine=1，refetchInterval: 存在 queued/running 项时 3000 否则 false）；JobsPage 表格列：名称/结构缩略(MoleculeView)/SMILES(截断+title)/StatusBadge/steps(stats.steps)与耗时(finished_at-started_at 短格式)/创建时间/操作（查看→/jobs/:id；running|queued 显取消；全部显删除，删除带 confirm()）；空态提示+去提交按钮；顶部导航（logo/提交入口/登出）

- [ ] **Step 1: 失败测试**（mock fetch 返回两个 JobOut fixture（一 running 一 succeeded）→ 行数=2、状态徽章渲染、取消按钮仅 running 行、删除触发 DELETE 且行消失（第二次 fetch 返回剩余））
- [ ] **Step 2: 实现 → 通过 → build**
- [ ] **Step 3: Commit** `feat: jobs list page with polling and actions`

---

### Task M2-T8: 任务详情页骨架（头部/轮询/Tab 容器/failed 态）

**Files:**
- Create: `frontend/src/pages/JobDetailPage.tsx`
- Test: `frontend/src/tests/job-detail-page.test.tsx`

**Interfaces:**
- Consumes: useJob（GET /api/jobs/{id}，refetchInterval: running|queued→3000）、useResult（enabled: status∈{succeeded,partial}）、Tabs、StatusBadge
- Produces: 页面结构：
  - 头部卡：名称、StatusBadge、SMILES（MoleculeView 预览）、统计行（steps/tokens_in/tokens_out/耗时/创建于）、partial 附 reason、failed 显 error 块
  - running/queued 形态：状态说明 + "本任务平均需要 10-30 分钟" + 轮询自动刷新提示（不实现命令流——M4）
  - succeeded/partial 形态：Tabs = 路线树 | 报告 | 文件（文件 Tab 本任务只放骨架：metrics 卡（n_nodes/n_edges/n_terminals））
  - cancelled：状态说明 + 重新提交按钮（跳 / 带该 SMILES 预填 query param）
  - 404（非本人/不存在）：错误卡 + 返回列表

- [ ] **Step 1: 失败测试**（三形态 fixture：running→无 Tabs 显等待文案；succeeded→Tabs 三项 + metrics 数字来自 /result；failed→error 文案展示）
- [ ] **Step 2: 实现 → 通过 → build**
- [ ] **Step 3: Commit** `feat: job detail page skeleton with status-aware layout`

---

### Task M2-T9: RouteTreeCanvas 路线树画布（核心任务）

**Files:**
- Create: `frontend/src/components/RouteTreeCanvas.tsx`、`frontend/src/components/NodeDrawer.tsx`、`frontend/src/lib/treeLayout.ts`
- Create: `frontend/src/tests/fixtures/visualization.json`（拷贝 examples/result_demo/export/visualization.json）
- Test: `frontend/src/tests/tree-layout.test.ts`、`route-tree-canvas.test.tsx`

**Interfaces:**
- Consumes: Visualization 类型、MoleculeView、@xyflow/react
- Produces: `RouteTreeCanvas({vis})`；`treeLayout(vis) → {positions: Map<nodeId,{x,y}>, order: string[]}`（纯函数，可单测）

- [ ] **Step 1: 布局纯函数 + 失败测试**

布局算法（全量实现）：

```ts
// lib/treeLayout.ts
import type { Visualization } from "../types";
export interface Positioned { x: number; y: number }
const X_GAP = 240, Y_GAP = 170, RXN_DY = 85;

export function treeLayout(vis: Visualization): Map<string, Positioned> {
  const children = new Map<string, string[]>();
  for (const e of vis.edges) {
    if (!children.has(e.source)) children.set(e.source, []);
    children.get(e.source)!.push(e.target);
  }
  const byId = new Map(vis.nodes.map((n) => [n.id, n]));
  const positions = new Map<string, Positioned>();
  let slot = 0;
  const visited = new Set<string>();
  const dfs = (id: string) => {
    if (visited.has(id)) return;
    visited.add(id);
    const node = byId.get(id);
    if (!node) return;
    const isRxn = node.type === "reaction";
    positions.set(id, { x: slot * X_GAP, y: node.depth * Y_GAP + (isRxn ? RXN_DY : 0) });
    if (!isRxn) slot += 1;               // 反应节点与产物同 slot，不占横向位
    for (const c of children.get(id) ?? []) dfs(c);
  };
  const roots = vis.nodes.filter((n) => n.type === "molecule" && n.role === "target");
  roots.forEach((r) => dfs(r.id));
  vis.nodes.forEach((n) => { if (!visited.has(n.id)) dfs(n.id); });  // 孤儿节点兜底
  // 居中：x 整体平移 -maxX/2
  const xs = [...positions.values()].map((p) => p.x);
  const shift = xs.length ? Math.max(...xs) / 2 : 0;
  positions.forEach((p) => (p.x -= shift));
  return positions;
}
```

`tree-layout.test.ts`（用真 fixture）：
- 返回覆盖全部 28 节点（17+11）
- target 节点（mol_0）y=0；rxn_1 y=85；mol_1/mol_2 y=170
- 所有 x 为 X_GAP 整数倍减 shift（或验证同 depth 分子节点 x 互异）
- 同一 mol 的前体经同一 rxn 连接（mol_0→rxn_1→{mol_1,mol_2}）都存在 position

- [ ] **Step 2: 画布组件 + 失败测试（mock MoleculeView 与 @xyflow 的交互断言用 DOM）**

组件实现要点（React Flow 12）：

```tsx
import { ReactFlow, Background, Controls, MiniMap, type Node, type Edge } from "@xyflow/react";
import "@xyflow/react/dist/style.css";
```

- `treeLayout(vis)` → 构造 RF Node[]：molecule 节点 = 自绘卡片（div：MoleculeView(160x110) + 底栏 role 徽章 + cs_score 两位小数），边框色按 role（target border-blue-600 bg-blue-50 / intermediate border-slate-400 bg-white / terminal border-emerald-600 bg-emerald-50）；reaction 节点 = 琥珀 pill（label 截断 18 字符，title 全文）
- Edge[]：type "smoothstep"，animated=false，targetArrow
- `onNodeClick` / `onEdgeClick` → setDrawer({kind:"node"|"edge", id})
- NodeDrawer（右侧滑出 w-96）：node=molecule → MoleculeView(320x220)+全字段（smiles 可复制、role、cs_score、classification、depth）；node=reaction → label、reaction_smiles 等宽换行；edge → source→target 名称与 type
- MiniMap pannable zoomable；fitView 初始；Controls

`route-tree-canvas.test.tsx`（真 fixture + vi.mock MoleculeView 为 span[data-testid=mock-mol] + jsdom 下 React Flow 需要 ResizeObserver —— setup.ts 提供 `global.ResizeObserver = class { observe(){} unobserve(){} disconnect(){} }`，Mock getComputedStyle 已由 jsdom 覆盖必要项）：
- 渲染后 mock-mol 数量 = 17
- reaction pill 文本含 "Reductive amination"
- 点击 terminal 节点卡片（fixture 中 mol_2）→ 抽屉出现且含其 SMILES
- 点击边 → 抽屉显示 type

- [ ] **Step 3: 接入 JobDetailPage 路线树 Tab（该 Tab 渲染 `<RouteTreeCanvas vis={result.visualization}/>`, visualization 缺省（incomplete）时显示"产物不完整"提示卡）**
- [ ] **Step 4: 全前端测试 + build**
- [ ] **Step 5: Commit** `feat: route tree canvas with bipartite layout and detail drawer`

---

### Task M2-T10: 报告 Tab（iframe）+ 文件 Tab（下载列表）

**Files:**
- Modify: `frontend/src/pages/JobDetailPage.tsx`
- Test: `frontend/src/tests/job-detail-page.test.tsx`（追加两用例）

**Interfaces:**
- Consumes: `/api/jobs/{id}/files/export/SYNTHESIS_REPORT.html`（iframe src，同域带不了 Authorization 头！——见 Step 1 裁定）、`stats.export_dir`
- Produces: 报告 Tab = `<iframe src=... className="w-full h-[70vh] border rounded">` + "新窗口打开"链接；文件 Tab = 固定清单下载链接：export/report.txt、export/tree.json、export/terminals.json、export/visualization.json、export/session.json、messages.jsonl、session.json（存在的才显示——实现方式：HEAD 请求探测或直接渲染 <a> 由浏览器 404 兜底；选后者+后缀图标）

**Step 1 裁定（写入实现注释）**：iframe/img 无法带 Bearer 头。方案：文件 Tab 的下载链接与报告 iframe 走**短期查询参数令牌**会引入新后端面——M2 不做；改为**同源依赖**：开发期 Vite proxy 同域、生产 Nginx 同域，且 M2 先放宽 files 端点为 `Bearer 或 ?token=<jwt>` 双通道（后端 3 行改动：files 函数读 `token` query param 等价校验）。测试：`?token=` 有效 200、无效 404。此改动随本任务一并提交（后端测试追加至 test_m2_result_files.py）。

- [ ] **Step 1: 失败测试**（后端 ?token= 两例 + 前端：succeeded fixture → 报告 Tab iframe src 含 /files/export/SYNTHESIS_REPORT.html?token=、文件 Tab ≥4 个 <a download>）
- [ ] **Step 2: 实现（后端 token 双通道 + 前端两 Tab）→ 通过 → build**
- [ ] **Step 3: Commit** `feat: report iframe and artifact downloads via token query channel`

---

### Task M2-T11: 全局导航/布局 + 手动联调 + 构建基线 + README

**Files:**
- Create: `frontend/src/components/Layout.tsx`（顶栏：Rachel-v2 Platform 标识 / 提交 / 任务 /（admin）供应商 / 登出 + 主体容器 max-w-7xl）
- Modify: `App.tsx`（页面套 Layout）、`backend/README.md`（前端开发/构建段落）、`frontend/.gitignore`（Vite 模板自带，确认 node_modules/dist 忽略、public/rdkit 不忽略）

**Interfaces:**
- Produces: 可运行的全站导航；README 增补：`cd frontend && npm install && npm run dev`（proxy 8000）、`npm test`、`npm run build`（产物 dist/ 交 Nginx，M5）

- [ ] **Step 1: 手动联调清单（执行者实际跑一遍，结果写进报告）**

```bash
# 终端1: D:/Anaconda/envs/rachel-v2/python.exe -m uvicorn app.main:app --port 8000 --app-dir backend
# 终端2: npm --prefix frontend run dev
# 浏览器 http://localhost:5173：
# 1) 注册 admin → 2) 提交页输入苯酚 c1ccccc1O（真实 RDKit 预览应显示结构）
# 3) 任务列表出现 running → 4) 详情页轮询 → mock LLM provider 下很快 succeeded
# 5) 路线树 Tab 渲染（真实 WASM 结构图）→ 6) 报告 Tab iframe → 7) 文件下载
# 8) 登出 → 守卫跳转
```
（无浏览器自动化要求——Playwright 属 M5；执行者以 curl + 构建通过 + 组件测试全绿替代目测不可达项时，在报告中如实注明哪几项经何种方式验证。）

- [ ] **Step 2: 全量回归**：后端全套 + 前端 `npm test -- --run` + `npm run build`
- [ ] **Step 3: Commit** `chore: global layout, integration checklist and docs for m2`

---

## Self-Review 记录

- **Spec 覆盖**：spec §5.1 五页面（login/register/提交/列表/详情）→ T3/T5/T6/T7/T8；§5.2 提交页双栏+示例+预览 → T6；§5.3 详情页三 Tab+running/失败形态 → T8/T9/T10；§5.4 技术栈逐项（React Flow 12、RDKit-JS 本地打包=public/rdkit、TanStack 轮询、Zustand）→ T3/T4/T7；报告 iframe+原始文件 → T10；§4.1 缺失的 /result 与 /files 端点 → T1/T2。M3（决策审计/终点审计面板）与 M4（实时进度/SSE/admin 页）不在本计划——正确留白。
- **占位符扫描**：无 TBD；T5-T8 的测试以"失败测试（要点清单）"描述+fixture 断言，实现代码在接口区给了完整形状——执行者按要点写测试属计划内动作。
- **类型一致性**：types.ts 为唯一类型源；RouteTreeCanvas 消费 Visualization（与后端 /result 契约一致）；treeLayout 签名与画布消费一致；token 双通道只在 T10 引入并回补测试。
- **风险标注**：① @rdkit/rdkit npm 可用性（兜底：unpkg 手动下载 vendored）；② React Flow 12 在 jsdom 需 ResizeObserver polyfill（已写入 T9）；③ iframe 鉴权（已裁定 query token 双通道）；④ driver export 落位变更可能影响既有测试——T1 Step5 明确要求全套回归。
